# 工夫点レポート — 旅費わりかん (Trip Splitter)

## 1. 小数点以下の処理(評価ポイントで明示されている点)
単純に「合計 ÷ 人数」を各自 `Math.round` すると、四捨五入の誤差が蓄積し
**「全員の負担合計」≠「実際の支出合計」** になるケースがある(例: 1000円を3人で割ると
333.33...円 × 3人 を丸めると 999円 or 1002円 になりうる)。

これを避けるため `splitAmount()` では:
1. `Math.floor(amount / n)` を全員のベースとする
2. 端数(余り)を **支出登録順に決定的に** 先頭からN人に1円ずつ配る

という方式にし、合計の誤差を数学的にゼロにしている。これはNode単体スクリプトで
「balancesの総和が常に0になる」ことを確認済み。

## 2. マイナス値・不正入力のバリデーション
金額は `validateAmount()` で「空文字・NaN・0以下・非有限値」を弾いている。
UI側でも `<input type="number" min="0">` で入力段階から抑制しつつ、
`min` 属性だけでは貼り付け入力等をすり抜けられるため、送信時に必ずJS側でも再検証する
二重チェックにしている(HTML属性だけに頼らない)。

## 3. 送金回数の最小化
単純な「全員が全員に払う」実装だとN人でN(N-1)/2件の送金が発生しうる。
実際の割り勘アプリの使い勝手を考えると、送金回数が少ないほど嬉しいため、
**最大の債権者と最大の債務者を毎回マッチさせる貪欲法** を実装し、
送金件数を最大 (人数-1) 件に抑えている。

## 4. 複数通貨対応とAPI障害への耐性
`exchangerate.host` は現在、無料利用でもAPIキー登録が必須になっている(課題作成時点との仕様差分)。
そこで、
1. APIキーが `.env`または`.env.local` にあれば `exchangerate.host` を使用
2. 失敗した場合(オフライン等)は等倍換算で「概算表示」に切り替え、アプリ全体を止めない

という構成にした。「外部APIが落ちてもアプリの主機能(国内旅行の割り勘)は使える」ことを優先した設計判断。

## 5. UI・UX面の工夫
- 参加者削除時、その人が関わる支払いがある場合は確認ダイアログを出す(データ消失の事故防止)。
- 支払いフォームは送信後も「支払った人・通貨・割り勘対象者」の選択を保持し、連続入力しやすくした
  (旅行中に複数の支払いをまとめて入力する場面を想定)。
- 精算通貨は支払いを1件でも登録すると変更不可にし、換算基準が旅行の途中でぶれないようにした。
- 参加者が0人のときは支払いフォーム自体を非活性にし、「先に参加者を追加してください」と明示して、何をすべきかわかりやすいようにした。
